{
  "buildCommand": "npm run build --workspace=packages/shared && npm run build --workspace=apps/admin",
  "outputDirectory": "apps/admin/dist",
  "installCommand": "npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
